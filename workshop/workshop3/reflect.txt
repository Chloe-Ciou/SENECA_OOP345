
I have learned,
1. How to implement class templates, if an array within a class template we must overwrite [] operator.
2. A template parameter may be any combination of: 
  (1)a type template parameter 
  (2)a non-type template parameter 
  (3)a template template parameter
3.typename and class are interchangeable in the basic case of specifying a template.
